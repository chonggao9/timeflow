// TimeFlow 设计令牌 —— 暖色亲和（珊瑚橙 + 米白底）
export const colors = {
  bg: '#FBF5EF',           // 暖米白底
  surface: '#FFFFFF',      // 卡片/弹层
  primary: '#FF6B6B',      // 珊瑚橙主色
  primaryStrong: '#F24E4E',// 按压/强调
  primarySoft: '#FFE9E4',  // 珊瑚浅色（选中态底）
  primarySofter: '#FFF4F0',// 更浅（图标底）
  ink: '#2B231E',          // 暖黑主文字
  ink2: '#7A6F66',         // 次级文字
  ink3: '#B6A99E',         // 弱文字/占位
  line: '#F1E7DD',         // 发丝分隔线
  line2: '#E9DCD0',        // 时间轴连线
  past: '#E8D9CB',         // 时间轴历史节点
  success: '#4CAE7F',
  danger: '#E08A7A',
};

export const radius = {
  lg: 22,
  md: 16,
  sm: 12,
};

export const shadow = {
  sm: {
    shadowColor: '#2B231E',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.05,
    shadowRadius: 4,
    elevation: 2,
  },
  primary: {
    shadowColor: '#FF6B6B',
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.35,
    shadowRadius: 12,
    elevation: 6,
  },
};
